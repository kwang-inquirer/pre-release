<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
        <fullName>District_Match__c</fullName>
        <description>District From and District Until match flag. If true then match on District From to match on Route's District. If false then alert is for all Routes.</description>
        <externalId>false</externalId>
        <formula>IF(isBlank(District_From__c),true,
    IF(isBlank(District_Until__c),true,
        IF(District_From__c == District_Until__c, true, false)
    )
)</formula>
        <inlineHelpText>District From and District Until match flag</inlineHelpText>
        <label>District Match</label>
        <trackTrending>false</trackTrending>
        <type>Checkbox</type>
    </CustomField>
